import React from 'react';

const Header = () => {
    return (
        <header>
            {/* Add any header-specific content here */}
        </header>
    );
};

export default Header;
